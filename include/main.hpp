#pragma once
#include <fstream>
#include <iostream>
#include <iterator>
#include <regex>
#include <string>
#include <vector>

std::vector<std::string> split(std::string str, std::string separator);
int main(int argc, const char **argv);